interface:
  display_name: "Check Release"
  short_description: "Verify release readiness for a project"
  default_prompt: "Use $check-release to verify release readiness and propose the next version."

policy:
  allow_implicit_invocation: false
