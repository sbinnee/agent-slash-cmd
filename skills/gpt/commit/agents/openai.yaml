interface:
  display_name: "Commit"
  short_description: "Create one clean git commit from staged changes"
  default_prompt: "Use $commit to create one commit from currently staged changes."
policy:
  allow_implicit_invocation: false
